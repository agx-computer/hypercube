{
  "name": "server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "deploy:demo": "wrangler deploy --minify --env demo",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@better-auth/api-key": "^1.6.23",
    "@hypercube/core": "workspace:*",
    "better-auth": "^1.6.23",
    "hono": "^4.12.30"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5",
    "wrangler": "^4.110.0"
  }
}
